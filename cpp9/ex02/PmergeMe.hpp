#pragma once

#include <iostream>
#include <string>


class PmergeMe {
	private:
		PmergeMe();
		PmergeMe(PmergeMe const &copy);
		PmergeMe &operator=(PmergeMe const &copy);
		~PmergeMe();
	public:
		
};